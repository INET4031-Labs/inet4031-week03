# TODO: This file is generated by kompose in Part 2, Step 10
# Students must fix the same two problems as flask-deployment.yaml:
# 1. Remove plaintext env vars (POSTGRES_DB, POSTGRES_USER, POSTGRES_PASSWORD)
#    Replace with: envFrom: - secretRef: name: postgres-credentials
# 2. Change strategy from Recreate to RollingUpdate
#
# NOTE: kompose's default `io.kompose.service` label was replaced with `app: postgres`.
# Week 4's OpenTofu resource and Week 7's NetworkPolicy/RBAC manifests select on
# `app:`, not `io.kompose.service:` -- keep this label scheme going forward.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: postgres:15-alpine
        ports:
        - containerPort: 5432
        envFrom:
        - secretRef:
            name: postgres-credentials
        volumeMounts:
        - name: db-data
          mountPath: /var/lib/postgresql/data
      volumes:
      - name: db-data
        persistentVolumeClaim:
          claimName: db-data
